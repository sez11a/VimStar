-- User plugins configuration for VimStar
-- This file is generated from vimstar-user-plugins-template during installation.
-- Location: ~/.VimStar/lua/user-plugins/init.lua (NOT directly in ~/.VimStar/lua/)
-- Add your custom Neovim plugins here without affecting git updates.

return {
  -- Example user plugin (uncomment and modify as needed):
  -- {
  --   "username/repository",
  --   config = function()
  --     require("repository").setup({})
  --   end
  -- }
}